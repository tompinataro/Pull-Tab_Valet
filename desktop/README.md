# Pull Tab Valet Desktop

## Local desktop run
1. Install desktop dependencies:
   - `npm run desktop:install`
2. Start desktop app in development:
   - `npm run desktop:dev`

## Build Mac desktop app
1. Export web assets from mobile and package:
   - `npm run desktop:build:mac`
2. Installer/output is generated under:
   - `desktop/dist/`
